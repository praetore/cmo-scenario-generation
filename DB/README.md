# Local DB folder (optional)

The `.db3` files here are **gitignored**. You do not need this folder if `cmo_config.ini` points at your CMO install’s `DB` directory (see root `README.md`).

To use a local copy instead, place `.db3` files here, for example:

```text
DB3K_515.db3
```
